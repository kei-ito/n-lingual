const console = require('console');
const {translate} = require('n-lingual');
console.log(translate('foo'));
console.log(translate('foo'));
console.log(translate('N points'));
console.log(translate());
require('./bar');
